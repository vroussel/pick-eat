[http]
ip = "0.0.0.0"
port = 4242

[db]
host = "localhost"
port = 5432
name = "pickeat"

[db.app_user]
name = "pickeat_app"
password = "${DB_PICKEAT_APP_PASSWORD}"

[db.migration_user]
name = "pickeat"
password = "${DB_PICKEAT_PASSWORD}"

[images]
storage_root = "${IMAGES_STORAGE_ROOT}"
